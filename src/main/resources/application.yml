spring:
  application:
    name: course
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  data:
    web:
      pageable:
        default-page-size: 20
        max-page-size: 100
